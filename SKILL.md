---
name: redbook-post-gen
description: 小红书图文（轮播笔记）一键生产 skill。当用户想做小红书图文贴、笔记、种草图文、爆款标题/钩子、图文带货/引流/获客帖，或提到"小红书发帖/起号/起标题/想个钩子/出套轮播图"等创作意图时，务必优先触发本 skill——即使用户没明说"生成"，只要意图是产出一篇小红书图文内容就触发。它串联选题→标题→钩子→实证检索→去 AI 味写稿→审稿→出图（3:4 轮播卡）→落盘全流程。边界：只做小红书图文贴，不涉及视频脚本、直播话术、评论区运营或投流，那些场景不要调用。
---

# 职责边界（总纲，强制）

主 skill 只做四件事：**流程控制、信息传递、最终结果输出、主线一致性自检**。

- 主 skill 禁止亲自产出任何专业内容：选题诊断、写稿方向、钩子、检索聚合、文案、审稿、出图，一律由对应依赖 skill 按其内部流程产出。
- 主 skill 基于上一步的输出整理成下一步需要的输入并传递；严格按 11 步顺序执行，禁止跳步、乱序。
- **主线一致性自检（强制）**：每一步结束后（无论是子 skill 产出后还是用户确认后），主 skill 必须对当前信息包中的 **热搜词 / 选题(=标题) / 钩子 / 行文方向 / 终稿** 与用户最初在步骤 1 提出的选题思路 + 步骤 2 明确的人群画像 + 图文贴目的进行强相关校验；任一字段跑题、偏题、弱相关或新增与主线无关的信息，必须立即退回对应子 skill 重产或打回用户重新输入，禁止携带偏题内容进入下一步。
- **子 skill 决策优先（强制）**：调用任一依赖时，读取其 SKILL.md 并完整遵循其内部流程与规范；**以该子 skill 的内部决策为主**，主 skill 不在子 skill 介入执行时做干预——不裁剪、不改写其判断逻辑，不替代其下结论，不自行补采子 skill 本应自行决定的信息（如署名、版式）。主 skill 的角色仅限四步：① 把上一步信息整理为子 skill 所需输入 → ② 启动并让其按自身规范执行 → ③ 原样接收其输出 → ④ 传递给下一步或交用户确认。

# 提问规范（全局，强制，适用于所有需要用户输入的环节）

- **禁止交互式弹窗提问**（不使用 AskUserQuestion 等交互组件）；所有提问在对话中以纯文本进行。
- **每次只问一个问题**；给出**编号选项**（1. 2. 3. …）；除问题与选项外**不输出多余信息**。
- 用户回答编号或自定义内容后，再进入下一环节或下一个问题。
- **确认闸门只输出结论（强制）**：所有需要用户确认的环节（步骤 3 / 4 / 7 / 8 / 10 / 11），主 skill 向用户呈现的内容**只包含最终结论**——选题、标题、钩子、行文方向、热搜词表、终稿全文等定稿结果；**禁止输出子 skill 的诊断过程、中间思考、公式匹配细节、五维诊断报告、生成方案列表、候选列表与排序理由等中间产物**。子 skill 的内部流程与中间产出仅供主 skill 做主线一致性自检，不呈现给用户。

# 环境检测（每次任务开始前，强制，禁止跳过）

> 任何任务步骤开始之前必须完成本节全部检测。任一必检项失败即中止并报告；禁止跳过检测直接执行，禁止用降级方案替代。

## 1. 依赖 skill 检测（安装 / 更新 / 记录）

依赖表（与 `references/dependencies.md`、`ensure_deps.sh` 三处一致，改动须同步）：

| 依赖 | 用途（流程步骤） | 安装位置（运行时解析） | 缺失时安装来源 |
|---|---|---|---|
| xhs-real-keywords | 热搜词采集（步骤 3） | `<skills_root>/xhs-real-keywords` | `lsuyu899-tech/xhs-real-keywords` 子路径 `skills/xhs-real-keywords`（免费、无 API、带来源证据、浏览器自动化） |
| dbs-content | 选题 + 行文方向 + 主标题草案（步骤 4） | `<skills_root>/dbs-content` | `dontbesilent2025/dbskill` 对应子路径 |
| dbs-hook | 钩子生成（步骤 4） | `<skills_root>/dbs-hook` | `dontbesilent2025/dbskill` 对应子路径 |
| dbs-xhs-title | 主标题定稿校验（步骤 4） | `<skills_root>/dbs-xhs-title` | `dontbesilent2025/dbskill` 对应子路径 |
| multi-search-engine | 实证数据检索（步骤 5） | `<skills_root>/multi-search-engine` | 本机已装，无自动安装来源 |
| content-deai-engine | 写稿与逐图文案（步骤 6、8） | `<skills_root>/content-deai-engine` | `lanyasheng/content-deai-engine` |
| dbs-resonate | 共鸣审稿（步骤 7、8） | `<skills_root>/dbs-resonate` | `dontbesilent2025/dbskill` 对应子路径 |
| no-ai-slop | 去 AI 味（步骤 7） | `<skills_root>/no-ai-slop` | `petergyang/no-ai-slop` 子路径 `skills/no-ai-slop` |
| guizang-social-card-skill | 图文卡出图（步骤 10） | `<skills_root>/guizang-social-card-skill` | `op7418/guizang-social-card-skill` |

**skills 根目录解析（运行时，不写死，避免死依赖）**：依赖 skill 一律安装/查找于「当前 agent 的 skills 根目录」，该目录**每次任务实时解析**，不在任何文件里写死：
- 优先取环境变量 `$AGENT_SKILLS_DIR`（若已设置）；
- 否则取本 skill 自身所在目录的父目录——即本 skill 被装入的 agent skills 根（如本 skill 位于 `<root>/redbook-post-gen/SKILL.md`，则 `<root>` 即根目录）。
- 这样无论运行在哪种 agent 环境（如 WorkBuddy 的 `~/.workbuddy/skills/`、Claude Code 的 skills 目录、项目本地 skills 目录等），都能自动适配。每个依赖的实际位置恒为 `<skills_root>/<name>/SKILL.md`，每次重新判定。

执行机制：运行 `<SKILL_REPO_DIR>/ensure_deps.sh`（bash）。脚本对依赖表逐项执行（skills 根目录由脚本按上述规则实时解析）：

1. **存在性（实时）**：`<skills_root>/<name>/SKILL.md` 是否存在，每次重新判定，不读历史缓存路径；存在 → 记录；缺失且有来源 → `git clone` 安装到当前 skills 根目录；缺失且无来源 → 记录 `missing-optional`（不阻塞，但对应步骤需要时按依赖失败处理）。
2. **最新性**：**仅 git 类依赖**（目标带 `.git`，如 guizang、content-deai-engine）用 `git ls-remote` 对比上游 HEAD 与本地 commit，过期 → **自动更新到最新**（fetch + reset，保留 `node_modules` 等未跟踪文件）；无法连接上游仅告警、不阻塞。**复制式依赖**（dbs 系列 / xhs-real-keywords / no-ai-slop，目标无 `.git`）**不做自动更新**：为免产生盘外状态文件，已禁用版本基线写入；如需刷新，删除其目标目录后重跑脚本。
3. **深度依赖**：`guizang-social-card-skill`（出图）与 `xhs-real-keywords`（浏览器采集热搜词）共用 Playwright 模块 + Chromium 二进制，检测点 `<skills_root>/guizang-social-card-skill/node_modules/playwright`、`<skills_root>/xhs-real-keywords/node_modules/playwright` 与 `chromium.executablePath()`；缺失自动安装/从 guizang 复制，自修复失败即中止。
4. **不写状态文件**：环境检测只做检查/安装/更新与深度依赖校验，**不再向本仓库写入任何状态文件**（无 `.deps-cache` / `deps.json`）；路径恒在运行时由 skills 根实时解析，不作任何路径持久化。
5. **超时与重试**：git 探测 180s / 克隆 300s / 拉取 180s / npm 60s / Playwright 900s；克隆与拉取最多重试 2 次；超时打印原因与修复方案，不无限挂起。

中止条件（脚本退出码非 0）：有来源却安装失败 / 深度依赖自修复失败 / git 依赖过期且自动更新失败 → 立即中止，报告具体原因。

## 2. 主机运行时检测（缺失自动安装到本机全局目录并自动更新）

| 项目 | 检测命令 | 通过标准 | 缺失 / 不足时策略 |
|---|---|---|---|
| Node.js | `node --version` | ≥ v18（guizang 渲染与 `validate-social-deck.mjs` 校验要求） | **自动安装到本机全局受管目录并更新到满足要求的版本**（install_binary / managed runtime，跨项目共享，不污染系统环境） |
| Python | `python --version` | 当前无任何依赖要求 Python；仅在未来某依赖声明需要时启用检测 | 同 Node.js 策略：安装到本机全局受管目录并自动更新 |
| git | `git --version` | 可执行（依赖安装与更新） | 报告缺失与安装建议，中止 |
| 网络 | 可达 `github.com` / `raw.githubusercontent.com` | 可达（依赖安装更新、实证检索、网络取图） | 报告并中止 |

- 运行时安装与更新由主 skill 调用本机受管运行时能力完成；每次任务开始前重新检测，版本不足即更新。
- 用户补齐环境后，从环境检测第 1 节重新开始。

# 流程（11 步，严格顺序，禁止跳步）

> **子 skill 决策优先贯穿全程（强制）**：下列每一步凡调用依赖 skill（步骤 3 / 4 / 5 / 6 / 7 / 10），均以该子 skill 的内部决策为主；主 skill 不在子 skill 介入执行时做干预，只传递信息、控制流程、接收结果。具体边界见「职责边界 · 子 skill 决策优先」与「内层自治」，违例见文末「禁令」。
>
> **主线一致性自检贯穿全程（强制）**：每一步结束后，主 skill 对信息包中的热搜词 / 选题(=标题) / 钩子 / 行文方向 / 终稿 与用户原始选题思路 + 人群画像 + 图文贴目的强相关校验；偏题即退回重产，禁止携带偏题内容进入下一步。

## 步骤 1：接收用户输入

用户提供基础选题思路（必填）与配图（可选）。主 skill 原样记录，不加工、不润色。

## 步骤 2：信息补全（主 skill 职责）

完整信息 = **选题思路 + 面向人群画像 + 图文贴目的（引流 / 获客 / 点赞收藏等）**。

- 逐项核对；每缺失一项，按提问规范问一个问题补齐（缺几项分几轮，每轮一题）。
- 选项须结合选题思路给出，禁止空泛模板。目的缺失时提问示例：「这篇图文贴的核心目的是？1. 引流（关注/私信） 2. 获客（咨询/成交） 3. 点赞收藏（曝光权重） 4. 其他（请直接说明）」。
- 三项齐全后整理为信息包底稿，进入步骤 3。
- **主线一致性自检（步骤 2 末）**：核对「选题思路 + 人群画像 + 图文贴目的」三项与用户在步骤 1 的原始输入一致；用户已确认即锁定为后续每步校验的基准。

## 步骤 3：热搜词采集（xhs-real-keywords）

- 主 skill 以「选题思路 + 人群画像 + 图文贴目的」提炼 1–3 个种子词，读取 xhs-real-keywords 的 SKILL.md 并按其 Workflow 调用采集脚本（默认 `node <skill_dir>/scripts/collect-xhs-keywords.mjs --industry "<种子词>" --depth 1 --max-seeds 1 --channel chrome --headless --profile-dir .cache/xhs-profile --out-dir <临时目录>`）。采集策略、搜索联想/大家都在搜回退、去重、证据抓取由该 skill 内部决定，主 skill 不干预，仅传入种子词、接收 JSON 结果。
- 接收**带来源证据**的关键词列表（每条含 `keyword` / `intent` / `sourceType` / `evidenceUrl` / `firstSourceSeed` / `sources`），主 skill 按与选题思路的相关度筛选 **5–10 个最贴近的搜索关键词**写入信息包，并保留各自来源证据链接（供步骤 4 主标题与步骤 6 正文自然融入、供用户核验）。
- **用户亲自确认闸门（强制，每次一题、编号选项）**：把筛选后的候选词以确认表呈现给用户——字段：关键词 | 来源类型 | 证据链接 | 意图。提问：「热搜词确认：1. 全部确认采用 2. 保留部分（请指出保留哪些 / 剔除哪些） 3. 重新采集（调整种子词）」。用户未明确确认前，**禁止进入步骤 4**。用户选 2 时按指定增删后再次呈现确认；选 3 时调整种子词重采一轮再确认。
- 采集结果不足 5 个时：调整种子词再采一轮；仍不足则如实呈现实际数量，交用户裁决（用户可显式放行「不足 5 个也采用」或要求继续重采），**禁止编造热词凑数、禁止用非小红书来源词冒充热搜词**。
- **主线一致性自检（步骤 3 末）**：核对热搜词与「选题思路 + 人群画像 + 图文贴目的」强相关；弱相关词剔除，重采或交用户裁决。
- 禁止：主 skill 编造热词、用非小红书来源词冒充热搜词、遮盖或伪造来源证据链接、跳过用户确认直接进入选题环节。

## 步骤 4：选题(=标题) / 行文方向 / 钩子 一站产出（dbs-content + dbs-hook + dbs-xhs-title）

- 目标：dbs 子 skill 内部决策后，**直接产出 ONE 套最优组合**（不再给用户多候选）：**1 个选题（封面主标题草案 4–14 字 / 含钩子或痛点 / 结构化呈现 / 禁生硬口号） + 1 个写稿方向（切入角度 / 内容结构 / 第一步行动） + 1 个钩子**。多候选的复杂度由 dbs 子 skill 在内部消化，主 skill 与用户只看最终一套。
- 主 skill 把「步骤 2 信息包 + 步骤 3 确认热搜词」打包，依次调用：
  1. **dbs-content**：按其 Phase 1→4 标准流程（接收内容 → 形式匹配「小红书图文」→ 五维诊断 → 报告），产出 1 套选题 + 写稿方向 + 主标题草案。
  2. **dbs-hook**：把同一信息包 + dbs-content 的选题 / 方向结论作为素材传入，按其「话题 + Hook + 可信度」公式与 Phase 4 三方法（素材提取 / 素材增补 / 悬念制造）产出 1 条与所选选题 + 方向最贴合的钩子。若其按 Phase 2 判定素材不足而停止优化，主 skill 把停止结论原样转达用户并回到步骤 2 补充素材，禁止强迫生成。
  3. **dbs-xhs-title**（主标题定稿校验）：把 dbs-content 产出的主标题草案传入，按其小红书标题公式做定稿校验（公式选择 / 排序 / 优化），主 skill 不干预其公式选择；校验通过即定稿，作为固定值供步骤 6 封面标题槽使用。
- 选题、写稿方向、钩子的具体结论由 dbs 子 skill 内部决策，主 skill 不干预、不替代其下判断。
- **主线一致性自检（步骤 4 末，强制）**：在呈现给用户前，主 skill 校验 **选题(=标题) / 行文方向 / 钩子** 与用户原始选题思路 + 人群画像 + 图文贴目的强相关；任一弱相关或跑题 → 退回对应 dbs 子 skill 重产并说明退回原因，禁止主 skill 自改。
- **用户呈现规则（强制）**：向用户呈现的内容**只包含最终结论**——选题（标题定稿值）、行文方向（一句话）、钩子（一句话）；**禁止输出** dbs-content 的五维诊断报告、dbs-hook 的候选列表与 Top3 推荐理由、dbs-xhs-title 的公式匹配过程与公式编号等中间产物。
- **用户一站确认闸门（强制，编号选项、一问）**：「步骤 4 一站确认：1 通过（采用该选题+标题+方向+钩子组合） 2 需调整（请指方向，例如换选题 / 换方向 / 换钩子）」。用户未明确确认通过，禁止进入步骤 5。
- 用户反馈调整 → 主 skill 把调整方向原样转交对应 dbs 子 skill 重产 → 再走主线一致性自检 → 再次呈请用户确认；**停滞即停**：若本轮 dbs 重产所针对的问题与上一轮用户给出的调整方向完全一致（dbs 改来改去都在处理同一个点而未推进），主 skill **立即停止循环**，向用户报告阻塞点（双方结论 + 上一轮调整方向），由用户裁决（指定新方向 / 接受现状 / 终止任务）。**禁止以「再试一轮」的方式继续内耗。**
- 禁止：主 skill 自行拟定选题 / 标题 / 方向 / 钩子、把多候选塞回用户、用非 dbs 产出的内容填充。

## 步骤 5：实证数据检索（multi-search-engine）

- 主 skill 分析「已确认选题(=标题) + 行文方向 + 钩子 + 人群画像 + 目的」，列出需要检索的实证清单：支撑论据的**真实数据（须标注来源）**、公开发布的观点信息、权威信息（官方发布 / 学术 / 行业报告）等一切有利于行文的信息。
- 调用 multi-search-engine 按其 Workflow（语言评估 → 受控检索 → 结果聚合）执行，接收**带来源链接**的检索报告，写入信息包。检索策略与结果聚合由该 skill 内部决定，主 skill 不干预。
- 分析结论为「无需检索」时，记录理由后直接进入步骤 6；需要检索而该依赖不可用 → 按依赖失败处理中止。
- **主线一致性自检（步骤 5 末）**：核对检索报告所获数据 / 观点 / 权威信息与主线（选题思路 + 人群画像 + 图文贴目的）强相关；偏离主线的检索结果剔除或重检索。
- 禁止把未经验证的来源当事实写入信息包；检索不到的信息标注「待核实」。

## 步骤 6：正文初稿与逐图文案（content-deai-engine）

主 skill 组装完整信息包传入 content-deai-engine：选题思路、人群画像、图文贴目的、热搜词、已确认选题(含主标题)、已确认行文方向、已确认钩子、步骤 4 确认标题（固定值，封面标题槽必须一致）、实证检索报告（带来源）、用户配图（如有）、目标平台=小红书、素材来源标注。

在传入信息包中**显式追加小红书图文贴结构与出图要求（属信息传递，非主 skill 自行写稿）**，要求 content-deai-engine 在标准流程之外额外完成三项：

1. **图片结构规划（由写稿 skill 决策，主 skill 不代定）**：结合本主题与小红书图文贴风格，分析并给出最佳图片数量与每图角色，固定骨架为——① 1 张封面（步骤 4 确认标题 + 钩子 + 配图说明）② 1 张开篇要点提炼 / 目录·总结 ③ n 张正文（逐字）④ 1 张末尾获客钩子（CTA）。n 的具体取值与每页切分点由 content-deai-engine 按正文量决定。
2. **逐图文案（图即文案，逐字不提炼）**：按上述骨架，为**每一张图**写出其上承载的**完整文字**——封面写标题（=步骤 4 确认值）+ 钩子句；开篇提炼 / 目录写本帖脉络（须与后续正文页逐页对应、数量一致，不得出现正文没有的要点，行文自然不生硬）；正文页逐字复制对应段落（不改写为分点、不丢字）；末尾钩子写获客 CTA。
3. **完整文字稿 + 逐图文案块**：标准输出（正文 / 评论区首评 / 标签建议 / 发布前风险提示）照常产出，并额外输出一份「逐图文案」结构化块——按图编号（封面 / 目录 / 正文①…正文n / 末尾钩子）列出每张图的完整文字内容，供步骤 10 出图直接采用。

- **字数约束声明（强制，来自 `config.md`，主 skill 调用写稿子 skill 前必读并原样转达）**：`config.md` 已按 guizang 画布（1080×1440、内边距 96/88）与固定字号（正文 28px）预先算定每页字符上下限。主 skill 必须把以下约束作为信息传递写进 content-deai-engine 的输入，**不得由主 skill 自行重算或改写数值**：
  - **每正文页**：约 150 字（120–200，大字号陈述页路线，见 `config.md` 第 2 节）；逐字不提炼、图即文案；低于 120 视为过空须补足，高于 200 由写稿子 skill 拆分或精简。版式（字号 / 行距 / 换行 / 留白）由 guizang 落实，主 skill 不重算。
  - **开篇要点提炼 / 目录**：条目须与正文页逐页对应、数量一致，不得出现正文没有的要点。
  - （封面主标题约束 4–14 字已在步骤 4 由 dbs-xhs-title 落实，本步骤封面标题槽须等于该确认值。）
- 执行：content-deai-engine 按自身流程落实；正文、标签、**图片结构、逐图文案、目录与获客钩子文案**均由 content-deai-engine 内部决策与撰写，主 skill 不干预、不替代其判断、不自行撰写或补写任何文案（含目录与每图内容）。
- 主 skill 接收标准输出 + 逐图文案块；核对封面标题槽 = 步骤 4 确认值，不一致退回 content-deai-engine 修正。
- **主线一致性自检（步骤 6 末）**：核对初稿与主线（选题思路 + 人群画像 + 图文贴目的）一致；标题槽 = 步骤 4 确认值；逐图文案与目录严格同源对应；不符即退回 content-deai-engine 重写。
- 约束传入：热搜词须自然融入正文（≥2–3 个）；数据与观点须对应检索报告来源，禁止臆造；无法验证信息标注「待核实」；开篇提炼 / 目录须与正文页严格同源对应（杜绝目录与正文脱节、数量不一致）。

## 步骤 7：审稿循环（dbs-resonate 共鸣诊断 + no-ai-slop 去 AI 味，主 skill 控制）

循环目标：dbs-resonate（共鸣）与 no-ai-slop（去 AI 味）**两者均认可**方退出；任一不认可则按其修改方向落实一轮后复审。

- **写稿记忆隔离（强制）**：调用 content-deai-engine 写稿或修订时，主 skill 仅向其传入「**本轮输入的信息与要求**」——包括：步骤 4 确认的选题/行文方向/钩子/标题、步骤 5 检索报告、步骤 6 产出（如修订轮）、本轮 dbs-resonate 或 no-ai-slop 给出的修改方向、用户最新修改方向。**禁止传入也不允许写稿 skill 引用任何前几轮的写稿产物（草稿 / 旧稿 / 旧逐图文案 / 历史版本任何字段）**；每轮写稿必须以本轮输入为唯一上下文重写，不沿用前轮措辞、结构、句式，避免对历史偏见的重复强化。同一修改方向连续出现时，写稿 skill 须从输入重新推导解法，而非基于上一稿「微调」。
1. **dbs-resonate 共鸣诊断**：主 skill 把当前稿交 dbs-resonate，按其 Step 1→4 执行（提取主张 → 核心机制审查 → 五维度共鸣诊断 → 诊断报告）。其【具体改法】即为**修改方向**，主 skill 原样转交 content-deai-engine 落实一轮修订。**dbs-resonate 仅负责共鸣 / 传播诊断，不负责去 AI 味（去 AI 味由 no-ai-slop 专责）。**
2. **no-ai-slop 去 AI 味**：同一轮修订后，主 skill 把稿交 no-ai-slop，按其行为准则重写去除 AI 味；接收其去 AI 味稿（或「无 AI 味」通过结论）。若判定仍有 AI 味，其重写稿即修改方向，主 skill 原样转交 content-deai-engine 吸收融合到稿中（含同步更新「逐图文案」块）。
3. 修订 / 去味稿再交 dbs-resonate 复审 → 复审后再交 no-ai-slop；如此循环，直至 dbs-resonate 五维全有效、无删除 / 强化建议（共鸣达标）**且** no-ai-slop 判定无 AI 味（去味达标）。
4. **停滞即停（强制）**：主 skill 对比本轮 content-deai-engine 产出与上一轮的「问题点 / 修改方向」——若**本轮改动所针对的问题与上一轮完全一致**（即 AI 改来改去都在处理同一个点而未推进），主 skill **立即停止审稿循环**，向用户报告阻塞点（双方结论 + 上一轮问题点），由用户裁决（指定新方向 / 接受现状 / 终止任务）。**禁止以「再试一轮」的方式继续内耗。**
5. 禁止主 skill 自行改稿；修改方向必须来自 dbs-resonate 或 no-ai-slop 报告，改稿 / 吸收必须经 content-deai-engine 落实。
- **主线一致性自检（审稿循环每轮末）**：核对修订 / 去味稿未引入与主线无关的新增信息、未改变选题方向；偏离即退回 content-deai-engine 修正。

## 步骤 8：用户最终确认（循环至通过）

向用户输出终稿全文（步骤 4 确认标题 + 正文 + 逐图文案结构 + 评论区首评 + 标签建议 + 来源标注），按提问规范问一题：「最终稿确认：1. 通过 2. 需修改（指出方向）」。

- **主线一致性自检（步骤 8 末）**：终稿与用户原始选题思路 + 人群画像 + 图文贴目的强相关；标题槽 = 步骤 4 确认值；逐图文案与目录同源对应；热搜词自然融入 ≥ 2–3 处。

用户提出修改 → 主 skill 把用户修改方向并入步骤 7 的循环（content-deai-engine 改、dbs-resonate 审共鸣、no-ai-slop 去 AI 味），修订稿再回到本步骤请用户确认；重复直至用户确认通过。

## 步骤 9：终稿文案落盘（用户确认通过后立即执行，与出图、正式落盘同目录）

- 用户最终确认通过后，主 skill **立即**把终稿文案落盘为 md 文件：`<SKILL_REPO_DIR>/output/<最终标题_YYYYMMDD>/<最终标题>.md`——与步骤 10 guizang 出图产物、步骤 11 正式落盘**同一子目录**，不另设草稿目录。
- **文件内容（终稿存档，全部原样写入）**：① 标题 ② **完整正文文案**——连贯完整的一篇文章（非按图分页的结构化文案），用于发布到小红书图文贴底部文字区；③ 评论区首评 ④ 标签建议 ⑤ 来源标注。逐图文案（按图编号）仅作为**附录**附在文件末尾，标注「出图素材 · 非发布正文」，供步骤 10 出图直接采用。
- 目的：文案与图片产物同目录共存；步骤 10 出图直接采用该 md 的逐图文案附录，步骤 11 落盘无需再复制；出图 / 改稿 / 中断后不丢失。
- 步骤 8 循环再次修改时同步更新该 md（同选题复用覆盖）。

## 步骤 10：图文贴生成（guizang-social-card-skill）

- 主 skill 先按提问规范问风格（一次一题）：「图文贴风格选择：1. Editorial Magazine × E-ink（电子杂志 · 图文混排） 2. Swiss International（瑞士国际主义）」；选定后再问主题色（Editorial：Ink Classic / Indigo Porcelain / Forest Ink / Kraft Paper / Dune / Midnight Ink；Swiss：IKB Blue / Lemon Yellow / Lemon Green / Safety Orange；编号列出）。风格与主题色仅影响 guizang 的视觉版式，不改变文案内容。
- 配图：用户已提供 → 作为素材传入；未提供 → 按 guizang 内层流程向用户一问（自备照片 / 网络取图 / AI 生成），主 skill 把该问原样转达为编号选项，禁止代答。
- **网络取图图内标注来源（强制，用户指定规则）**：用户选择网络取图时，外部来源配图（摄影平台 / CC 授权 / 博主图等）**必须在图内标注来源**（摄影师 / 平台 / 授权类型，落版由 guizang 按其规范执行）；主 skill 把「图内标注来源」作为用户明确要求的出图要求传入 guizang（属信息传递，不属设计注入），并把完整来源同步写入 `assets/SOURCES.md`。AI 生成、用户自备图不适用。
- 主 skill 把落盘子目录 `<落盘根目录>/output/<最终标题_YYYYMMDD>` 作为 guizang 的 task folder **显式传入**（覆盖其默认 `local-tests/<slug>/`），使渲染产物直接落入输出目录。**task folder 内部结构固定**：源配图放 `assets/`（guizang 规则）；渲染产物统一放 **`images/`** 子目录（**覆盖 guizang 默认 `output/` 命名——用户指定的输出目录优先**），即 `<task folder>/images/card-*.png`。
- **逐图文案传递（来自步骤 6 的 content-deai-engine 输出）**：主 skill 把步骤 6 产出的「逐图文案」结构化块**原样**传给 guizang，作为出图的内容素材。每张图承载的完整文字、封面 / 目录 / 正文 / 末尾钩子的角色划分、图片数量与切分，**已由 content-deai-engine 在步骤 6 决定**，主 skill 不重新切分、不改写、不补写、不代定结构。（该逐图文案已在步骤 6 受 `config.md` 字数约束——封面标题 4–14 字由步骤 4 落实、正文页约 150 字（120–200）；密度与字号交由 guizang 按其 M04 大字号 recipe 执行，主 skill 不重算。）
- 按其 Intake → Extract Story → Choose Style Mode → Plan Pages → Build & Render → Image/Screenshot Handling → Deliver 全链路执行；小红书 3:4（1080×1440）轮播卡，视觉版式（字号层级、留白、配图位置、recipe 选择、密度处理）由 guizang 按其规范与内容规划决定，主 skill 不干预。
- **正文完整还原规则（强制，覆盖 guizang 默认压缩）**：guizang 的 `content-planning.md` 默认采用 Compression Ladder 并明写 "Do not put the full article on images"——**本流程不适用该默认**。主 skill 在 Intake 明确指令：每张卡片须承载步骤 6 逐图文案中的**完整逐字文字**，禁止提炼为分点 / 金句 / 清单后出图（即"图即文案"）。封面承载标题 + 钩子，目录 / 总结卡承载 content-deai-engine 已写好的脉络文字，正文页承载逐字段落，末尾钩子卡承载获客 CTA 文案。
- **主 skill 不向卡片注入自定义设计元素（强制）**：署名（byline）、刊头文案、自定义版式、自定义配图说明等一律由 guizang 按其规范决定；出图全权交 guizang，主 skill 不自行写入需用户补充的占位、不干预其版式与署名决策。主 skill 仅传入逐图文案、配图、风格与主题色，其余交给 guizang。**注：传入 content-deai-engine 已产出的逐图文案（含封面 / 目录 / 正文 / 末尾钩子角色划分）属信息传递，不属本禁令所指的设计注入。**
- 出图与校验完全按 guizang 自身流程执行（含其「先展示、用户要求时再自动校验」的交付节奏），主 skill 不附加自创的版式 / 目录结构 / 校验时机要求。
- **主线一致性自检（步骤 10 末）**：核对出图卡片标题槽 = 步骤 4 确认值；逐图文字与步骤 6 逐图文案块完全一致；目录 / 正文页数对应；来源标注已写入 `assets/SOURCES.md`。
- 类目落在其能力边界外被拒单 → 按依赖失败处理中止，禁止回退默认方案。

## 步骤 11：落盘

- 落盘前按提问规范问一次目录（一次一题）：「落盘位置：1. 默认（skill 内部 `output/`） 2. 自定义（请给出绝对路径）」。
- 步骤 9 起文案已与出图产物同目录，默认即 `<SKILL_REPO_DIR>/output/<最终标题_YYYYMMDD>/`；用户选自定义时，把该整个子目录移动到自定义根目录下 `<自定义>/output/<最终标题_YYYYMMDD>/`。
- **落盘目录结构（固定规则）**：`<落盘根目录>/output/<最终标题_YYYYMMDD>/` 下共三类内容，全部原样保留——① `<最终标题>.md` 终稿文案（步骤 9 落盘）② `images/` 全部卡片 PNG（步骤 10 渲染产物）③ `assets/` 源配图与来源记录（如有）。
- 产物清单：最终稿文案 md（步骤 9 已落盘，同目录）+ 全部卡片 PNG（`images/`）+ 网络取图来源记录（`assets/`）。
- **发布检查清单（交付附赠）**：落盘完成后，主 skill 按 `references/publish-checklist.md` 逐项自查一次，把未通过项如实提示用户（不阻塞交付）；清单覆盖封面首图 3 秒抓人、正文首屏、CTA 位置、合规来源、发布设置。
- 同选题再次修改复用原目录覆盖更新；中间产物（渲染脚本副本 / html / temp）禁止留在落盘目录。
- 终态向用户输出：文字区全文 + 图片 + 落盘子目录路径。

# 依赖失败处理（强制）

- 环境检测失败、依赖目录或 SKILL.md 缺失、依赖报错 / 返回不可用 / 超时 / 能力边界拒单、信息包被退回且无法补全 → 立即中止全部执行，报告具体原因。
- 禁止回退到任何默认方案或替代路径。
- 依赖 skill 自身给出默认 / 兜底方案时 → 按提问规范询问用户是否采纳；未经用户明确选择，禁止自动采纳或拒绝。

# 内层自治（强制）

- 调用任一依赖，完整遵循其 SKILL.md 规范流程；**以子 skill 内部决策为主，主 skill 不在其介入执行时做干预**（详见「职责边界 · 子 skill 决策优先」）。
- 主 skill 仅负责外层编排、信息收集与传递、接收依赖返回与最终落盘；禁止在主 skill 内重写依赖职责（自行写稿、自行审稿、自行出图、自行诊断、自行检索聚合、自行改稿、自行决定版式/署名）。（dbs-resonate 仅做共鸣 / 传播诊断，去 AI 味由 no-ai-slop 专责，主 skill 不把去 AI 味职责推给 dbs-resonate 或自行去味）。
- 各 dbs 子 skill 内部的 `/dbs*` 路由导航仅作原始形态保留，本流程已由主 skill 接管全部路由，不得令流程脱离主编排。

# 禁令

- ❌ 禁止跳过环境检测（依赖安装 / 更新 / 记录、主机运行时）直接执行任务。
- ❌ 禁止跳步、乱序，或未经用户确认越过步骤 4 / 7 / 8 的确认闸门。
- ❌ 禁止主 skill 亲自产出专业内容（选题诊断、写稿方向、钩子、检索聚合、文案、审稿、出图）。
- ❌ 禁止选题(=标题) / 写稿方向 / 钩子 / 终稿 与用户原始选题思路 + 人群画像 + 图文贴目的 偏题跑题（每步末主线一致性自检强制，任一弱相关或新增与主线无关信息即退回重产或打回用户）。
- ❌ 禁止编造热搜词、数据、来源；未验证信息必须标注「待核实」；禁止未经用户亲自确认热搜词即进入步骤 4（步骤 3 确认闸门强制），禁止遮盖或伪造来源证据链接。
- ❌ 禁止审稿循环中主 skill 自行改稿；修改方向必须来自 dbs-resonate 报告，改稿必须经 content-deai-engine 落实。
- ❌ 禁止交互式弹窗提问；禁止一次多问；禁止提问时输出与问题无关的信息。
- ❌ 禁止在用户确认环节输出子 skill 的诊断过程、中间思考、公式匹配细节、五维诊断报告、候选列表与排序理由等中间产物；确认闸门只输出最终结论（选题 / 标题 / 钩子 / 行文方向 / 热搜词表 / 终稿全文等定稿结果）。
- ❌ 禁止在子 skill 介入执行时干预其决策；子 skill 的内部判断与结论为主，主 skill 仅传递信息、控制流程、接收结果（详见「职责边界 · 子 skill 决策优先」）。
- ❌ 禁止主 skill 向卡片注入自定义设计元素（署名 / byline / 刊头文案 / 自定义版式 / 自定义配图说明等）；出图全权交 guizang，主 skill 不自行写入需用户补充的占位、不干预其署名与版式决策、**不代定图片结构与每图文案**。**传入 content-deai-engine 已产出的「逐图文案」（含封面 / 目录 / 正文 / 末尾钩子角色划分）属信息传递，不属本禁令的设计注入。**
- ❌ 禁止把去 AI 味职责交给 dbs-resonate（或主 skill 自行去味）；去 AI 味由 no-ai-slop 在步骤 7 审稿循环中专责，dbs-resonate 仅做共鸣 / 传播诊断。

- ❌ 禁止未经用户确认落盘目录即落盘；禁止把产物（output/）提交或推送到 GitHub。
