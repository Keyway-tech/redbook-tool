# 出图规格与字数约束（固定常量）

> 唯一权威来源。所有数值已按 guizang-social-card-skill 种子模板 `assets/template-editorial-card.html` 与校验器 `validate-social-deck.mjs` 的硬指标**预先算定并固定**。主 skill 每次任务直接引用本文件，**禁止运行时重复计算、禁止改写数值**。

## 1. 画布与版式基线

| 项 | 值 | 来源 |
|---|---|---|
| 画布尺寸 | 1080 × 1440（小红书 3:4 轮播卡，xhs） | guizang 规范 |
| 内容内边距（xhs） | 上下 96px / 左右 88px | `.poster.xhs .content { padding: 96px 88px; }` |
| 正文可用区 | 宽 904px（1080−176）× 高 1248px（1440−192） | 由内边距推算 |

## 2. 正文页（`.body`）字数约束

| 项 | 值 | 说明 |
|---|---|---|
| 固定字号 | 28px（line-height 1.65） | guizang 可读下限 22px 之上，舒适可读 |
| 每行字符 | 32 字 | 中文全角口径（904 ÷ 28 ≈ 32.3）；混合内容实际约 30–33 字/行 |
| 每页行数 | 27 行 | 满版（1248 ÷ 46.2 ≈ 27.0） |
| 单页满版上限 | 864 字 | 32 × 27 |
| **单页最少字数（密度硬指标）** | **648 字** | 864 × 75%，满足 guizang R5 密度 ≥75%（4 带密度均值 ≥0.745） |
| 溢出红线 | > 864 字须复核 guizang R1（scrollHeight > clientHeight） | 超出即溢出 |

**调用契约**：主 skill 调用写稿子 skill 时，必须声明**每正文页 648–864 字**；低于 648 视为密度不足须补足，高于 864 须确认不溢出 R1。正文逐字不提炼、图即文案。

## 3. 标题字数约束

### 3.1 封面主标题（`.h-display`，124px / line-height 1.06）

| 项 | 值 | 说明 |
|---|---|---|
| 每行字符 | 7 字 | 904 ÷ 124 ≈ 7.3 |
| 行数上限 | 2 行 | guizang R6 硬上限（xhs `maxLines: 2`） |
| **硬上限** | **14 字** | 7 × 2；guizang R6 标注 `maxChars: 8`/行，按展示宽度取 7/行更稳 |
| 推荐区间 | 6–12 字 | 单行 7 字最佳，双行 ≤14 |
| 下限 | 4 字 | 过短则封面空洞 |

### 3.2 每页小标题（`.h-md`，56px / line-height 1.18，参考）

| 项 | 值 |
|---|---|
| 每行字符 | 16 字（904 ÷ 56） |
| 行数上限 | 2 行 |
| 上限 | 32 字；推荐 ≤ 18 字 |

> 注：主标题指封面 `.h-display` 大标题；每页小标题（h-md）为辅助，不计入主标题约束。

## 4. 主 skill 调用契约（信息传递，非设计注入）

- **封面主标题约束（4–14 字）在步骤 9 由 `dbs-xhs-title` 落实**：主 skill 调用 dbs-xhs-title 前必须在本文件读取该约束并原样写入其输入（不得自行重算或改写数值）——**4–14 字（推荐 6–12，硬上限 14）**；必须含钩子 / 痛点，结构化呈现，禁止生硬口号。确认值固定，供步骤 10 封面标题槽使用。
- **正文页与目录约束在步骤 10 由 `content-deai-engine` 落实**：主 skill 调用 content-deai-engine 前必须在本文件读取以下约束并原样写入其输入（不得自行重算或改写数值）：
  - 每正文页：**648–864 字**，逐字不提炼、图即文案；低于 648 补足，高于 864 确认不溢出。
  - 开篇要点提炼 / 目录：条目须与正文页逐页对应、数量一致，不得出现正文没有的要点。
  - 封面标题槽须等于步骤 9 确认值。
- **调用出图子 skill（guizang）时**，主 skill 仅原样传入已受限的逐图文案；密度、字号层级、留白由 guizang 按其规范执行，主 skill 不重算、不干预。
- 数值如需调整（如更换正文固定字号），只改本文件，SKILL.md 引用不变；改后须同步复核上下限计算。
