# 出图规格与字数约束（固定常量）

> 唯一权威来源。所有数值已按 guizang-social-card-skill 种子模板 `assets/template-editorial-card.html` 与校验器 `validate-social-deck.mjs` 的硬指标**预先算定并固定**。主 skill 每次任务直接引用本文件，**禁止运行时重复计算、禁止改写数值**。

## 1. 画布与版式基线

| 项 | 值 | 来源 |
|---|---|---|
| 画布尺寸 | 1080 × 1440（小红书 3:4 轮播卡，xhs） | guizang 规范 |
| 内容内边距（xhs） | 上下 96px / 左右 88px | `.poster.xhs .content { padding: 96px 88px; }` |
| 正文可用区 | 宽 904px（1080−176）× 高 1248px（1440−192） | 由内边距推算 |

## 2. 正文页（`.body`）字数与版式约束

| 项 | 值 | 说明 |
|---|---|---|
| 版式路线 | **大字号陈述页**（statement page） | 一页一意、一段一页；以字号 / 行距 / 换行 / 留白占据画布，而非堆字数 |
| **每页正文字数** | **约 150 字** | 上限 200（防溢出 R1/R8），下限 120（防过空） |
| 每行字符 | 约 20 字 | 按字号 44px 折算（904 ÷ 44 ≈ 20.5）；实际 18–22 字/行 |
| 每页行数 | 7–8 行正文 | 150 ÷ 20 ≈ 7.5；由 guizang 按其 recipe 落实 |
| 正文字号 | 36–48px（推荐 44px） | guizang 默认 body 24px、模板明确允许 INLINE-tune 放大；行高 1.7–1.9 |
| 段落 / 换行 | 允许显式换行（<br>）拆短句 | 短句分段制造呼吸感，是占满空间的编排手段之一 |
| 密度口径 | guizang R5 为 WARN（非 FAIL） | 本路线的留白属 guizang M04「有意留白（hero statement）」语义，允许 ≤60% 内容覆盖；由 guizang 补锚点（kicker 章号 / 底部来源行 / 发丝线）消除空荡感；不缩画布、不堆装饰 |

**调用契约**：主 skill 调用写稿子 skill 时，必须声明**每正文页约 150 字（120–200）**，并声明版式路线为大字号陈述页；低于 120 视为过空须补足，高于 200 由写稿子 skill 拆分或精简。正文逐字不提炼、图即文案。

## 3. 标题字数约束

### 3.1 封面主标题（`.h-display`，124px / line-height 1.06）

| 项 | 值 | 说明 |
|---|---|---|
| 每行字符 | 7 字 | 904 ÷ 124 ≈ 7.3 |
| 行数上限 | 2 行 | guizang R6 硬上限（xhs `maxLines: 2`） |
| **硬上限** | **14 字** | 7 × 2；guizang R6 标注 `maxChars: 8`/行，按展示宽度取 7/行更稳 |
| 推荐区间 | 6–12 字 | 单行 7 字最佳，双行 ≤14 |
| 下限 | 4 字 | 过短则封面空洞 |

### 3.2 每页小标题（`.h-md`，56px / line-height 1.18，参考）

| 项 | 值 |
|---|---|
| 每行字符 | 16 字（904 ÷ 56） |
| 行数上限 | 2 行 |
| 上限 | 32 字；推荐 ≤ 18 字 |

> 注：主标题指封面 `.h-display` 大标题；每页小标题（h-md）为辅助，不计入主标题约束。

## 4. 主 skill 调用契约（信息传递，非设计注入）

- **封面主标题约束（4–14 字）在步骤 4 由 `dbs-xhs-title` 落实**：主 skill 调用 dbs-xhs-title 前必须在本文件读取该约束并原样写入其输入（不得自行重算或改写数值）——**4–14 字（推荐 6–12，硬上限 14）**；必须含钩子 / 痛点，结构化呈现，禁止生硬口号。确认值固定，供步骤 6 封面标题槽使用。
- **正文页与目录约束在步骤 6 由 `content-deai-engine` 落实**：主 skill 调用 content-deai-engine 前必须在本文件读取以下约束并原样写入其输入（不得自行重算或改写数值）：
  - 每正文页：**约 150 字（120–200）**，逐字不提炼、图即文案；低于 120 视为过空须补足，高于 200 由写稿子 skill 拆分或精简。
  - 版式路线：**大字号陈述页**——一页一意、短句分段、允许显式换行；字号 / 行距 / 留白由 guizang 按其 M04 大字号 recipe 落实，主 skill 不重算、不干预。
  - 开篇要点提炼 / 目录：条目须与正文页逐页对应、数量一致，不得出现正文没有的要点。
  - 封面标题槽须等于步骤 4 确认值。
- **调用出图子 skill（guizang）时**，主 skill 仅原样传入已受限的逐图文案；密度、字号层级、留白由 guizang 按其规范执行，主 skill 不重算、不干预。
- 数值如需调整（如更换正文固定字号），只改本文件，SKILL.md 引用不变；改后须同步复核上下限计算。
